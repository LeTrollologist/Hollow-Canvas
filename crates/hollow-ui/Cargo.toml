[package]
name = "hollow-ui"
version = "0.13.0"
edition = "2021"
description = "Egui-based studio interface, tools dock, layers manager, and color engine for Hollow Canvas"

[lib]
test = false

[dependencies]
hollow-core = { path = "../hollow-core", version = "0.13.0" }
hollow-io = { path = "../hollow-io" }
egui = { workspace = true }
glam = { workspace = true }
log = { workspace = true }
