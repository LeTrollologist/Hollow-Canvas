[package]
name = "hollow-ui"
version = "0.15.0"
edition = "2021"
description = "Immediate-mode user interface, studio panels, palette docks, and modals for Hollow Canvas"

[lib]
test = false

[dependencies]
hollow-core = { path = "../hollow-core", version = "0.15.0" }
hollow-render = { path = "../hollow-render", version = "0.15.0" }
hollow-io = { path = "../hollow-io", version = "0.15.0" }
egui = { workspace = true }
glam = { workspace = true }
log = { workspace = true }
